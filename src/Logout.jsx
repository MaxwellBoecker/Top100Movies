import React from 'react';

const Logout = () => (
  <div style={{ color: 'white' }}>
    <a href="/auth/logout">
      <button type="button">Logout</button>
    </a>

  </div>
);

export default Logout;
